/*global sap*/
sap.ui.define(
  [], function(){
function getServerInfo(){
  function handle(fResolve, fReject){
    //communicate with the worker
  }
  return new Promise(handle);
}
function getReponse(theBody){
  function handle(fResolve, fReject){
    //communicate with the worker
  }
  return new Promise(handle);
}
  });
